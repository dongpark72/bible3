{
    "name": "bible",
    "version": "1.0.0",
    "description": "Bible Web App",
    "scripts": {
        "build": "mkdir -p dist && cp public/index.html public/styles.css public/sw.js public/manifest.json dist/ && cp -r public/js public/assets public/icons public/data dist/",
        "deploy": "wrangler deploy",
        "db-init": "wrangler d1 execute bible-db --file=workers/schema.sql --remote -y"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}